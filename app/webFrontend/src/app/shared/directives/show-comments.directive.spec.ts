import { ShowCommentsDirective } from './show-comments.directive';

describe('ShowCommentsDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowCommentsDirective();
    expect(directive).toBeTruthy();
  });
});
